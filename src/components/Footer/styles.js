import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  footer: {
    color: "grey",
    backgroundColor: "black",
    textAlign: "center",
    alignItems: "flex-end",
  },
}));
